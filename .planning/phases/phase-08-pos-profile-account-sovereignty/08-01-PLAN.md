---
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260410000000_add_pos_pin_to_profiles.sql, apps/dashboard/src/lib/pos-actions.ts]
autonomous: true
---

# Plan 01: Database & Backend Foundation

Adds the necessary schema and server-side logic for POS terminal authentication.

## Tasks

<task>
<read_first>
- [profiles schema](file:///Users/jj/Downloads/project1/supabase/migrations/20260408000004_hr_schema.sql)
- [existing pos actions](file:///Users/jj/Downloads/project1/apps/dashboard/src/lib/pos-actions.ts)
</read_first>
<action>
Create a new Supabase migration `supabase/migrations/20260410000000_add_pos_pin_to_profiles.sql`:
```sql
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS pos_pin text;
COMMENT ON COLUMN profiles.pos_pin IS '4-digit numeric PIN for POS terminal access.';
```
Then, implement `verifyPosPin` in `apps/dashboard/src/lib/pos-actions.ts`.
</action>
<acceptance_criteria>
- Migration file exists and contains the `ALTER TABLE` statement.
- `apps/dashboard/src/lib/pos-actions.ts` exports `verifyPosPin`.
</acceptance_criteria>
</task>

<task>
<read_first>
- [apps/dashboard/src/lib/pos-actions.ts](file:///Users/jj/Downloads/project1/apps/dashboard/src/lib/pos-actions.ts)
</read_first>
<action>
Implement `updatePosPin(pin: string)` in `pos-actions.ts` to allow users to set their PIN initially.
</action>
<acceptance_criteria>
- `updatePosPin` is exported and updates the `pos_pin` column for the current user.
</acceptance_criteria>
</task>
